#spring.application.name=timesheet
server:
  port: 8080
spring:
  application:
    name: timesheet

    datasource:
      url: jdbc:h2:mem:test
      username: sa
      password:
      driver-class-name: org.h2.Driver
  jpa:
    show-sql: false